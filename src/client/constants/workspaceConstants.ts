export interface WorkspaceConstants {
    id: string;
    name: string;
}

export interface Project {
    id: string;
    name: string;
}

export interface Styleguide {
    id: string;
    name: string;
}
